const array = [];

for (let index = 0; index < array.length; index++) {
  const element = array[index];

}

const promises = [];
for (let item of array) {
  const element = promises.push(array[index]);

}

await Promise.all(promises);

